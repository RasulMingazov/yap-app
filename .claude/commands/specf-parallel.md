---
description: Analyze remaining tasks and recommend the next safe parallel implementation batch.
argument-hint: "[feature] [--active-phase N] [--max-workers N]"
disable-model-invocation: true
---

Read `.claude/skills/specf/SKILL.md` completely and follow it as the authoritative workflow.

Use these workflow arguments:

```text
parallel $ARGUMENTS
```
